import 'package:flutter/material.dart';

class AppImageLogo extends StatelessWidget {
  const AppImageLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/image_logo.png');
  }
}
